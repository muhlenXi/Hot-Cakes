```swift
let fruit: [String] = ["apple", "banana", "orange"]
    
var numbers: [Int] = [1, 2, 3, 4, 5]
    
numbers.append(6)
numbers.insert(0, at: 0)
    
numbers.remove(at: 0)
numbers.removeLast()
numbers.removeFirst()
numbers.removeAll()
```